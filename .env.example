# Copy to .env (gitignored) and fill in. wiskill loads this automatically from
# the config file's directory (and the current directory). Never commit .env.

# Signs the web session cookie. Generate a strong random value:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
WISKILL_SECRET="rBYk8-sBYha_q5yDlO43oYfLdW18Hrt4vaEi_MKuEm8"

# API key for the MCP server / JSON API. Create one with:
#   wiskill apikey add my-key --role editor
# Leave empty for trusted-local MCP (falls back to the editor role).
WISKILL_API_KEY=
