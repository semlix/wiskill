# Copy to .env on the server (never commit). Fill in a strong secret.

# Signs the web session cookie:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
WISKILL_SECRET=

# Image tag to run (matches the GHCR release tag).
WISKILL_TAG=latest
